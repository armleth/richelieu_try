apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd.yaml
  - vault.yaml
  - external-secrets.yaml
  - external-secrets-config.yaml
  - vault-config.yaml
  - bbox.yaml
  - cert-manager.yaml
  - cert-manager-config.yaml
  - cloudnative-pg.yaml
  - authentik.yaml
  - media.yaml
  - nextcloud.yaml
  - homepage.yaml
  - code-server.yaml
  - lathibandolaise.yaml
  - dbgate.yaml
  - actual-budget.yaml
  # Monitoring stack temporarily disabled to free RAM on the memory-constrained
  # node (~7.5 GiB). This prunes kube-prometheus-stack (Prometheus + Grafana),
  # prometheus-blackbox-exporter, scaphandre and monitoring-config, including
  # their PVCs/data. To re-enable once more RAM is available, simply uncomment
  # the two lines below. The application definitions in monitoring.yaml /
  # monitoring-config.yaml are left untouched.
  # - monitoring.yaml
  # - monitoring-config.yaml
